<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="44dp"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <!-- Left zone border (CL=blue, EL=orange, Rel=red, neutral=transparent) -->
    <View
        android:id="@+id/leftBorder"
        android:layout_width="4dp"
        android:layout_height="match_parent"
        android:background="@android:color/transparent" />

    <!-- Position number -->
    <TextView
        android:id="@+id/tvPosition"
        android:layout_width="28dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="12sp"
        android:textColor="?attr/colorOnSurface" />

    <!-- Team crest -->
    <ImageView
        android:id="@+id/imgTeamCrest"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_placeholder_crest"
        android:layout_marginStart="4dp"
        android:layout_marginEnd="4dp" />

    <!-- Team name — expands to fill remaining space -->
    <TextView
        android:id="@+id/tvTeamName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="start|center_vertical"
        android:textSize="12sp"
        android:ellipsize="end"
        android:maxLines="1"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:textColor="?attr/colorOnSurface" />

    <!-- MP — visible in FULL and SHORT -->
    <TextView
        android:id="@+id/tvMp"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="12sp"
        android:textColor="?attr/colorOnSurface" />

    <!-- W — FULL only -->
    <TextView
        android:id="@+id/tvWins"
        android:layout_width="28dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="12sp"
        android:textColor="?attr/colorOnSurface" />

    <!-- D — FULL only -->
    <TextView
        android:id="@+id/tvDraws"
        android:layout_width="28dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="12sp"
        android:textColor="?attr/colorOnSurface" />

    <!-- L — FULL only -->
    <TextView
        android:id="@+id/tvLosses"
        android:layout_width="28dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="12sp"
        android:textColor="?attr/colorOnSurface" />

    <!-- GF:GA — FULL only -->
    <TextView
        android:id="@+id/tvGoals"
        android:layout_width="54dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="11sp"
        android:textColor="?attr/colorOnSurface" />

    <!-- GD — SHORT only -->
    <TextView
        android:id="@+id/tvGD"
        android:layout_width="36dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="12sp"
        android:textColor="?attr/colorOnSurface"
        android:visibility="gone" />

    <!-- Form badges container — FORM only (fixed 155dp = 5 × 28dp badges + 4 × 3dp gaps) -->
    <LinearLayout
        android:id="@+id/formContainer"
        android:layout_width="155dp"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="center"
        android:visibility="gone" />

    <!-- Points — always visible, bold -->
    <TextView
        android:id="@+id/tvPoints"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="?attr/colorOnSurface"
        android:paddingEnd="4dp" />

</LinearLayout>
